using UnityEngine;

public class MedusaOP : MonoBehaviour
{
    public Dia dialogue;
    public int index = 0;
    public _CutsceneT CT;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
